
// This component is no longer needed with Next.js 15 App Router
// Hreflang tags are now handled directly in the metadata of each page layout
export default function HreflangTags() {
  return null;
}
